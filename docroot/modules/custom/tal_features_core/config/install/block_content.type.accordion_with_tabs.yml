langcode: en
status: true
dependencies:
  module:
    - workbench_moderation
third_party_settings:
  workbench_moderation:
    enabled: true
    allowed_moderation_states:
      - published
      - draft
      - needs_review
    default_moderation_state: draft
id: accordion_with_tabs
label: 'Accordion with Tabs'
revision: 1
description: 'Create Accordion component and place them on any page.'
