langcode: en
status: true
dependencies:
  module:
    - workbench_moderation
third_party_settings:
  workbench_moderation:
    enabled: true
    allowed_moderation_states:
      - published
      - draft
      - needs_review
    default_moderation_state: draft
id: banner
label: Banner
revision: 1
description: 'Adds a "Banner" reusable component.'
