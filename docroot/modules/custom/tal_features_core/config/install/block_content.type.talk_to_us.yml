langcode: en
status: true
dependencies:
  module:
    - workbench_moderation
third_party_settings:
  workbench_moderation:
    enabled: true
    allowed_moderation_states:
      - published
      - draft
      - needs_review
    default_moderation_state: draft
id: talk_to_us
label: 'Talk to us'
revision: 1
description: 'Adds a "Talk to us" reusable component.'
